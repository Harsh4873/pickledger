# PickLedgerPro morning upcheck — automation prompt (v2, 2026-07-19)

Supersedes the pre-redesign prompt. Updated for the per-market MLB/WNBA variant split, the mlb_team_total model, the inning-model research-only status, and the stale-only deploy readiness gate.

---

Perform the daily production upcheck for PickLedgerPro.

Guardrails and preflight
- Work only in /Users/harshdave/Documents/Harsh/split-repos/pickledger and verify its origin is Harsh4873/pickledger. Do not use /Users/harshdave/Documents/Harsh/PickLedgerPro; that is the user-site repository. Read AGENTS.md and, if present, the automation memory file.
- Compute TARGET_DATE once with TZ=America/Chicago date +%F and use it explicitly in every date-sensitive command and comparison.
- Never manually open the deployed site, a browser/UI, a live link, rendered Pages output, preview, screenshot, or dist output for verification. Approved scraper processes may use their own internal headless transport; do not inspect rendered pages.
- Preserve unrelated local changes. Verify gh api user --jq .login returns Harsh4873 and set/verify the repository Git identity as Harsh4873 with the matching noreply email before any publisher or push. Never add AI/Codex/Cursor taglines or co-author trailers.
- Fetch and fast-forward main only when safe. Never stash, reset, rebase, overwrite, or revert user changes. If the canonical worktree is dirty, on the wrong branch, or cannot fast-forward safely, leave it untouched and use a clean temporary clone/worktree from origin/main.
- All data writers share pick-cache-writer. Before dispatching, find any queued/in-progress writer, record its exact run ID, and wait for it rather than launching a duplicate. Capture the exact ID of every dispatched run and inspect that run after waiting, not an ambiguous latest run.
- Dispatch each workflow at most once per upcheck, with one additional rerun allowed only after a directly relevant code fix. Bound waits: model 100 minutes, player props 55 minutes, external feeds 30 minutes, deploy 30 minutes. End as HEALTHY, DEGRADED_EXTERNAL_BLOCKER, or CODE_FAILURE; do not loop indefinitely.
- **Deploy verification contract (critical):** a green deploy-pages.yml run does NOT mean the site deployed — the readiness job can defer "without failing," leaving the deploy job skipped (a ~10s run instead of ~30s+). Always inspect the run's JOBS and confirm the deploy job itself executed for the exact final HEAD. The readiness gate defers only on STALE data (latest date < TARGET_DATE); data dated ahead of TARGET_DATE is ready, not stale. In the overnight window before the morning props cron, today's player-props file may legitimately not exist yet — that is the one readiness blocker this automation should clear by dispatching player-props-refresh.yml (once), not by waiting.

1. Scores24 local publication
After preflight, run scripts/scrapers/scores24_publish_local.sh --date "$TARGET_DATE" before the remaining refresh checks because Scores24 blocks GitHub-hosted runner IPs. The publisher may clone, commit, push, and dispatch Pages itself. After it finishes, safely fetch/fast-forward to origin/main.
Independently validate scores24_wnba, scores24_mlb, and scores24_fifa_world_cup in the committed TARGET_DATE model cache. Each bucket must be ok, dated exactly TARGET_DATE, identify generatedBy=local:external-feed-refresh with meta.from=local, and cover the official America/Chicago slate by normalized matchup identity, never by a Scores24 URL date. Report expected, matched, missing, and published-pick counts for each. A zero-pick bucket is valid only when expected=matched=0 and missingMatchups is empty or the fully matched slate contains no editorial choice. Diagnose/fix a deterministic scraper or publisher defect; clearly classify an evidenced upstream block without weakening checks.

2. Team models and other external feeds
Required in-house team buckets for TARGET_DATE: mlb_new, mlb_inning, mlb_first_five, wnba, nba, nba_playoffs, nba_summer, fifa_world_cup — plus **mlb_team_total, mls, and nfl, which are soft-launched**: validate each bucket exists and is ok (mlb_team_total rows carry market=team_total; mls emits ML/spread/total from the FIFA-derived engine; nfl is shadow-mode and must render nowhere on the site), but their absence or failure must not by itself fail the upcheck (they are deliberately excluded from site_upcheck REQUIRED_MODEL_KEYS and the freshness guard).
Team BET and LEAN are tracked/displayed picks; team PASS may remain in raw cache for diagnostics but must not enter Team Home, Search, Trends, Best Bets, counts, source/sport rankings, period records, streaks, or grading inputs.
**Per-market variant contract (post-redesign):** the site splits team buckets into per-market sources at load time — mlb_new → "MLB ML"/"MLB Total"; mlb_first_five → "MLB F5"/"MLB F5 Total"; wnba → "WNBA ML"/"WNBA Spread"/"WNBA Total"; mlb_inning → "MLB Inning"; mlb_team_total → "MLB Team Total". Two records are protected and must never be emptied by a filter change: "MLB Total" (consensus-era history, epoch MLB:mlb_team_consensus_v1) and "WNBA ML" (full all-time history, 35-11 baseline as of 2026-07-19). All other in-house MLB/WNBA variants restart from 2026-07-19; external feeds and other sports keep full all-time records. If a rankings-filter or source-label change is ever suspected, simulate the filter against committed data/model_cache days before shipping.
**Model-specific expectations:** mlb_inning is research-only — no per-inning run market exists at DraftKings-via-ESPN, so its rows always carry the assumed -120 and can never be financially priced; its validation-lean publications require a positive calibrated edge and calibrated probability at least 1pp above the pick's own price. mlb_team_total rows should get real "Team Total Runs" prices attached for exact team+line matches (odds_source=posted_market) when DK has posted matching lines; it publishes strict-only (no validation-lean lane) and is expected to publish nothing until its walk-forward ledger reaches ~30 decided real-price picks. wnba emits three markets from one bucket with real scoreboard prices; its spread market requires 3.5+ cover margin and 6%+ vig-removed edge for BET, totals require a 7+ point model-to-line gap and 4.5%+ edge and stay capped at LEAN. wnba is in TEAM_PROP_MODEL_KEYS — verify its BET/LEAN rows receive pregame-ledger snapshots.
Validate every SportyTrader and SportsGambler split bucket for NBA, WNBA, MLB, and FIFA World Cup directly; do not trust the workflow conclusion alone because a partial feed refresh can exit successfully. Confirm the official known matchup slate is established before scraping and that soccer specialty markets and Asian handicap lines preserve their identity without false consensus or incorrect grading.
**Forebet feeds (soft-launched 2026-07-20):** validate the forebet_mlb, forebet_wnba, and forebet_mls buckets for TARGET_DATE — each must be ok and slate-matched by normalized matchup identity with kickoff-window disambiguation (series opponents repeat daily on Forebet league pages). A zero-pick bucket is valid only when officialMatchups=0 (off-day) or every official matchup is listed in unpublishedMatchups with a plausible cause. Forebet runs on GitHub runners (no Cloudflare block observed); if a run reports a Cloudflare error, classify as an external blocker like a Scores24 IP block — do not weaken checks. Their absence or failure must not by itself fail the upcheck (they are excluded from site_upcheck required sets).
**Covers feeds (soft-launched 2026-07-21):** validate the covers_experts_mlb, covers_experts_wnba, covers_computer_mlb, covers_consensus_mlb, covers_consensus_wnba, and covers_props_mlb buckets for TARGET_DATE — each must be ok and slate-matched with the same kickoff-window convention (Covers league pages attach stale prior-meeting expert cards to upcoming matchups; the scraper rejects them by tracking-datetime, meta.staleCards counts them). Per-engine expectations: experts rows carry covers_author/covers_author_url and per-author source labels ("Covers · <Name>" — a new author auto-registers as a new ranked source, no code change); computer publishes up to 3 game-market rows per matched matchup (ML/total/spread, odds only at the board's own line); consensus publishes majority-ML rows only at ≥55% and ≥50 votes, so "below vote floor"/"no clear majority" unpublished reasons are healthy; props (MLB only — Covers runs no WNBA props page or computer board, so covers_computer_wnba/covers_props_wnba do not exist) publishes 4★ BAT X board rows with scope=player and external_player_feed=true, rendering in Player mode, never Team. Coverage note: full-slate matchup discovery rides the Covers odds hub's current-day view, which aligns with TARGET_DATE at the scheduled refresh hours; overnight runs may match a subset ("not listed on Covers" reasons) — partial coverage with accurate meta is healthy, silent slate mismatch is not. All engines publish pregame-only with same-day carry-forward (meta.carriedForward — settled rows must never vanish when Covers drops finished-game cards). Covers hosts show no Cloudflare (CloudFront/IIS); classify any future challenge as an external blocker. Their absence or failure must not by itself fail the upcheck (excluded from site_upcheck required sets).
If today's team cache is missing or a required model failed, wait for any existing model-cache-refresh.yml run or dispatch it once, then wait up to 100 minutes. If non-Scores24 feeds are stale/failed, wait for or dispatch external-feed-refresh.yml once, then wait up to 30 minutes. Re-fetch main after each successful writer.

3. Full player-props ML path
Validate today's isolated NBAPlayerProps, MLBPlayerProps, and WNBAPlayerProps buckets under data/player_props_cache. Verify the outcome-ledger/train-refresh path and the MLB/WNBA joblib artifacts plus metadata.
For every published MLB/WNBA prop, require probability_source=player_props_ml_v1; ml_probability, ml_edge, ml_expected_value, ml_model_version, ml_market_family, ml_rank, baseline_projection; market_priced=true; odds_source=posted_market; a nonempty real odds value; and scope=player. Confirm ML expected value drives order, ungradeable markets remain unpublished, and synthetic/default-price props are not published when real markets exist. Confirm configured expanded MLB/WNBA family support separately from the market families actually observed today.
ML-owned props must bypass the old calibration shrink while remaining available to the outcome-ledger reliability layer. Publish at most eight qualifying props per game and at most one per player under the current contract; fewer is correct when fewer pass the quality gates.
Never accept MLBPlayerProps merely because date/current and ok=true. Determine the official MLB scheduled-game count; if it is greater than zero, zero published MLB props is not healthy even when abstained=true. Do not fabricate/synthesize picks to satisfy the count. A provider with no posted real markets after one refresh, or only one-sided/special-event board formats that cannot satisfy an active validated policy, is DEGRADED_EXTERNAL_BLOCKER when API payload shape, provider coverage, and gate diagnostics support it. Report the simultaneous internal quality abstention; do not relabel model disagreement or failed validation as a parser bug.
If today's props cache is missing/stale, a required bucket failed, scheduled MLB has zero picks, artifacts/metadata are missing, or a market-priced prop lacks the ML schema, wait for any existing player-props-refresh.yml run or dispatch it once, then wait up to 55 minutes. Diagnose deterministic problems in market feeds/parsers, the ML scorer, grading, artifacts, or workflow; rerun only after a concrete relevant fix.

4. Workflows, isolation, Pages, and tests
Inspect exact main-branch run IDs/conclusions/logs for model-cache-refresh.yml, player-props-refresh.yml, external-feed-refresh.yml, model-cache-freshness-guard.yml, auto-grade.yml, calibration-refresh.yml, and deploy-pages.yml. For every deploy-pages run, distinguish deploy-executed from readiness-deferred (see the deploy verification contract above). The player-props workflow is expected to end non-green after preserving diagnostics when scheduled MLB has zero published picks; classify from the committed cache and logs before deciding whether one post-fix rerun is warranted. Cannon daily was intentionally removed; use model-cache-freshness-guard.yml for that health check (note: the guard's required set intentionally excludes mlb_team_total). Do not dispatch calibration merely because it has not run today; run it only when due or after a relevant calibration fix.
Confirm GitHub Pages build_type=workflow through the API.
Run npm run upcheck and focused smoke tests: player props and merge/outcome history, cache contracts, grading, static viewer, external-feed scrapers, site upcheck, plus the redesign guardrails — tests/smoke/test_mlb_inning_guardrails.py, test_mlb_team_total.py, test_market_odds.py, test_mlb_team_consensus.py, test_wnba_guardrails.py. Do not open built/rendered output. Confirm Player mode supports NBAPlayerProps, MLBPlayerProps, and WNBAPlayerProps for rankings/grading and all player-prop records remain excluded from Team mode/rankings.

5. Fix, publish, deploy, report
Only fix a reproduced deterministic code/workflow defect. Make the smallest scoped change, run focused tests and npm run upcheck, commit on main with a plain human message and verified Harsh4873 author, then push main. For every coding change, dispatch deploy-pages.yml and verify through Actions/API that the deploy job itself executed (not merely that the run concluded green) for the exact final HEAD.
For data-only updates, verify a deploy-pages.yml run whose deploy job executed exists for the exact final HEAD; dispatch it once only if no queued/running/deploy-executed run covers that HEAD. Never create duplicate deploys.
Write only durable factual findings to automation memory; no secrets or bulky logs. Report terminal state, TARGET_DATE and final HEAD; Scores24 WNBA/MLB/FIFA counts and slate completeness; Forebet MLB/WNBA/MLS matched-vs-official counts; Covers per-engine counts (experts by author, computer, consensus, props) with matched-vs-official coverage; per-variant team pick counts for TARGET_DATE (MLB ML/Total/F5/F5 Total/Inning/Team Total, WNBA ML/Spread/Total, MLS ML/Spread/Total on matchdays) with pricing provenance; NBA/MLB/WNBA prop counts and scheduled-game count; configured versus observed ML family coverage and artifact versions/activation; exact workflow IDs/conclusions with deploy-executed status; fixes/commit/deploy; and any remaining blocker with evidence.
