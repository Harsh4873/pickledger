# Render deployment is disabled. PickLedger runs fully local via macOS launchd agents.
# Backend: com.pickledger.pickgrader.server.plist (pickgrader_server.py on 127.0.0.1:8765)
# Scores24: scores24_local_sync.py via launchd
# SportyTrader: sportytrader_local_sync.py via launchd
# SportsGambler: sportsgambler_local_sync.py via launchd
