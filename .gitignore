logs/*.log
.env
.env.*
!.env.example
.env.local
*.env.local
node_modules/
dist/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Local Python virtualenv
.venv/

# Tennis training data: the third-party tennis-data.co.uk archive and the spine
# derived from it. Never committed — rebuild with
# `python -m TennisPredictionModel.tennis_data`. Only the trained artifacts in
# TennisPredictionModel/artifacts/ are versioned.
data/tennis/raw/
data/tennis/processed/

# Cached API data
data/wnba_*.json

# Rebuildable CFB source datasets. Daily serving downloads only the seasons it
# needs; the trained model bundle and validation metadata are versioned under
# CFBPredictionModel/artifacts/.
data/cfb/

# Rebuildable MLB player-rating research datasets and isolated artifacts.
# These are retained by the manual GitHub Actions research workflow, never
# loaded by the public pick pipeline.
MLBPredictionModel/data/mlb_player_rating_oracle_dataset.csv
MLBPredictionModel/artifacts/mlb_player_rating_challenger*.joblib
MLBPredictionModel/artifacts/mlb_player_rating_challenger*_metadata.json
MLBPredictionModel/artifacts/mlb_player_rating_oracle_challenger*.joblib
MLBPredictionModel/artifacts/mlb_player_rating_oracle_challenger*_metadata.json

# Local config with API keys
config.py

# Render config (moved to docs/)
/render.yaml

# OS artifacts
.DS_Store
.DS_Store?
**/.DS_Store
Thumbs.db

# LaTeX build artifacts
*.aux
*.fls
*.fdb_latexmk
*.toc
*.bbl
*.blg
*.synctex.gz
**/out/

# Browser profiles (never commit)
.chromium_profile/

# Recovery snapshots
.recovery/

# Local agent worktrees/cache
.claude/

# Python bytecode (at any depth)
**/__pycache__/
*.pyd

# Local SQLite databases and state snapshots
*.db
*.db-shm
*.db-wal
*.db.bkp
pickledger_state.json

# One-off fix/migration scripts
fix_*.py

# Manual data feed snapshots
*_manual_feed.json

# Firebase / admin credentials and local config
pickledgerpro-firebase-admin.json
firebase.json
.firebaserc
serviceAccount*.json
AGENTS.md
